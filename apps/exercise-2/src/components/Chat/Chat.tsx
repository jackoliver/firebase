import { ChatInput } from './ChatInput';
import { ChatMessage } from './ChatMessage';
import { ChatWindow } from './ChatWindow';

export const Chat = {
  Message: ChatMessage,
  Input: ChatInput,
  Window: ChatWindow,
};
